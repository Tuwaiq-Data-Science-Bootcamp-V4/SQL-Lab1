# SQL-Lab1
- Create a table "BooksAuthors" containing two fields (AuthorId, BookId)
The query 1 :

<img src="1.png" width="500" height="500">

- Insert at least 5 records into the BooksAuthors table.
The query 2 :
<img src="2.png" width="500" height="500">
  
- Write a statement that will select the Country column from the Authors table.
  The query 3 :
<img src="3.png" width="500" height="500">
- Select all the different values from the Country column in the Authors table.
  The query  4 :
  <img src="4.png" width="500" height="500">
- Write an SQL query to return only Authors whose name begins with S.
  The query  5 :

  <img src="5.png" width="500" height="500">
- List the number of Authors in each country.
  The query  6 :
  <img src="6.png" width="500" height="500">
- Select all records from the Authors table, and sort the result alphabetically by the column's name.
  The query  7 :
  <img src="7.png" width="500" height="500">
- Select all records from the Authors table, and sort the result reversed alphabetically by the column name.
  The query  8 :
  <img src="8.png" width="500" height="500">
- Select all records where the Title column has the value ‘Great ' from the Books table.
  The query  9 :
  <img src="9.png" width="500" height="500">
- Use the NOT keyword to select all records where the country is NOT "USA".
  The query  10 :
  <img src="10.png" width="500" height="500">
- Select all records where the country column has the value 'USA' or ‘India' from the “Authors” table
  The query  11 :
  <img src="11.png" width="500" height="500">
- Select all records where the age column has the value BETWEEN 50 - 60 in the “Authors” table.
  The query  12 :
  <img src="12.png" width="500" height="500">
- Use the MIN function to select the record with the smallest value of the Age column from the “Authors” table.
  The query  13 :
  <img src="13.png" width="500" height="500">
- Choose the correct `JOIN` clause to view all books and their authors.
  The query  14 :
  <img src="14.png" width="500" height="500">
